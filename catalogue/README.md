# Common catalogue

The catalogue of the component's own meta-events: reads and exports of the
trail, registrations, profile and preset changes, key destruction, legal
holds, digest writing and verification, writer lifecycle and the daily
clock-synchronisation event. Every deployment carries it. Application
catalogues live next to the application that emits them and are registered
at deploy; see `docs/reference/catalogue.md`.
