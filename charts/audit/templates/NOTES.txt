{{ include "audit.fullname" . }} is installed.

The writer keeps {{ len .Values.profiles }} profile(s): {{ keys .Values.profiles | sortAlpha | join ", " }}.
Records reach it at {{ include "audit.sinkURL" . }}.

{{ if eq .Values.mode "stream" -}}
Stream mode: {{ .Values.replicas }} receiver(s) publish to {{ .Values.stream.name }},
and {{ .Values.writer.consumers }} writer(s) consume it through the durable consumer
{{ .Values.stream.consumer }}. The receivers hold neither the bucket nor a key.
A writer gathers for {{ .Values.roll.interval }} or {{ .Values.roll.maxRecords }} records,
whichever comes first, before it writes.
{{- else -}}
Direct mode: the front door is the write path. Every batch is put before it is
acknowledged, and how many objects a day becomes is decided by the emitter's own
batch size and flush interval. `mode: stream` puts a receiver in front and the
writers behind a durable consumer, which is what makes a writer that is down a
backlog rather than a hole.
{{- end }}

{{ if .Values.prefix -}}
The archive is {{ .Values.bucket }} under {{ .Values.prefix }}/. Four identities
need rights beneath that prefix, and none of them may delete anything:

  writer   put objects; put and get retention; put legal holds; get and list
           under the prefix; encrypt with the bucket's key
  digest   get and list under the prefix; put under {{ .Values.prefix }}/digest/;
           decrypt; sign with the digest key
  verify   get and list under the prefix; put under {{ .Values.prefix }}/verified/;
           decrypt
  query    get and list under the prefix; decrypt

Scope each to the objects under {{ .Values.prefix }}/ in {{ .Values.bucket }}, and
condition its s3:ListBucket on that prefix, or one application can read
another's archive. See docs/operations/s3-guide.md for the policies.
{{- else -}}
No `prefix` is set, so this installation writes at the root of {{ .Values.bucket }}.
That is only safe where the bucket is its own; in a shared one, `prefix` is what
keeps two applications' archives apart and what their IAM is scoped to.
{{- end }}

{{ if not (include "audit.hasDatabase" .) -}}
No database is configured. The writer indexes nothing, deduplicates in process,
and may run only one replica. Searching what it writes needs `database`.
{{- end }}

{{ if eq .Values.keys.provider "local" -}}
{{ if not .Values.keys.local.persistence.enabled -}}
The key directory is disposable. Pseudonyms will change on every restart and the
trail will not link across one. This is not a production install.
{{- else -}}
Pseudonymisation keys are wrapped into {{ .Values.keys.local.persistence.existingClaim | default (printf "%s-keys" (include "audit.fullname" .)) }}.
They are random, not derived: back that claim up, and never delete it to
reclaim space. Losing it is losing every tenant's pseudonyms.
{{- end }}
{{- end }}

{{ if .Values.query.enabled -}}
The query service answers at http://{{ include "audit.fullname" . }}-query:{{ .Values.query.service.port }}
for callers signed in by {{ range $i, $e := .Values.query.grants.issuers }}{{ if $i }}, {{ end }}{{ $e.url }}{{ end }}.
{{- if .Values.query.resolve.enabled }} It may resolve pseudonyms for callers an explicit rule allows.{{ end }}
{{- end }}

Check what a profile keeps, with the audit CLI (the images have no shell and
the writer's has no CLI in it):
  kubectl get configmap {{ include "audit.fullname" . }}-deployment -o jsonpath='{.data.deployment\.yaml}' > deployment.yaml
  audit profile explain <name> --deployment deployment.yaml

Verify the chain yourself, with nothing but the archive and the public key:
  audit verify --profile <name> --from <date> --to <date> --bucket {{ .Values.bucket }} --public-key <file>
