# The smallest install the chart will render: no stream, no database, one
# writer, and the jobs that need neither. A trial, so the writer takes writes
# from callers it cannot verify.
bucket: audit-archive
anonymousWrites: true
externalIdentifiersAreOpaque: true
jobs:
  digest:
    signingKey:
      existingSecret: audit-signing-key
  verify:
    publicKey:
      existingSecret: audit-signing-public
  purge:
    enabled: false
  clockSync:
    ntp: [time.cloudflare.com]
